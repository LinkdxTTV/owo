Title: dir or mkdir

Command: owo -dir directoryName || owo -mkdir directoryName

BodyStart:
    creates a new empty directory at location

    example usage: (will make a folder in actions called subactions)
        owo actions -mkdir subactions

BodyEnd:

Notes:
also made in owo hehe
directory names should not start with -
directory names should not have spaces in them
this is easy to break. i know. its hacky